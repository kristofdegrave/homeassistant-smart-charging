# Work type: `documentation` — the `home-assistant` overlay

Stack material for the `documentation` work type, read with the label's core files — and the
branch files they route to — as one work file, one bar and one checklist. Each core file's
**Overlays** section says which section below it takes. Nothing here restates a rule of those
files: every entry names the rule or the bar item it extends. The shape of an overlay is this
tree's `README.md`'s.

## Implement

**`system-design.md`'s service classifications** — its work file's step 3, with this stack's
examples: a **Client** is a consumer of the system such as HA automations/UI/entities; a
**Resource** is the external thing itself — the charger, HA entity state, the tariff/captar
source.

## Done

Nothing: both bars judge the decomposition and the plan against the analysis documents, not
against the stack.

## Review

Nothing beyond the bars.
