# Pinned after first resolve (Task 0.1). homeassistant and
# pytest-homeassistant-custom-component must stay compatible — phcc tracks HA releases.
homeassistant==2026.7.2
psutil==7.2.2
pytest
pytest-cov
pytest-homeassistant-custom-component==0.13.346
# The one ruff pin: ci.yml's lint job installs the version named here. 0.16 also formats
# Python code blocks inside Markdown, which the skills and docs are not written for; moving
# past 0.15 is a decision of its own, not a routine bump.
ruff==0.15.22
